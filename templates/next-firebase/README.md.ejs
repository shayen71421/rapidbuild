# <%= config.projectName %>

Generated with `rapidbuild`.

## Stack

- Next.js App Router
- TypeScript
- Tailwind CSS
- Firebase Auth
- Firestore

## Getting Started

1. Copy `.env.example` to `.env.local`.
2. Add your Firebase web app credentials.
3. Run the development server:

```bash
<%= config.packageManager %> run dev
```

Open http://localhost:3000.

## Firebase

Create a Firebase project, enable Email/Password and Google authentication, and create a Firestore database. Deploy rules with:

```bash
firebase deploy --only firestore:rules
```

Generated auth features include sign in, sign up, Google Sign-In, password reset, email verification, profile updates, route guards, and protected dashboard/profile routes.
