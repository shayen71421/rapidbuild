# <%= config.projectName %>

Generated with `rapidbuild`.

## Stack

- Next.js App Router
- TypeScript
- Tailwind CSS
- Firebase Auth
- Firestore

## Getting Started

1. Copy `.env.example` to `.env.local`.
2. Add your Firebase web app credentials.
3. Run the development server:

```bash
<%= config.packageManager %> run dev
```

Open http://localhost:3000.

## Firebase

Create a Firebase project, enable Email/Password authentication, and create a Firestore database. Deploy rules with:

```bash
firebase deploy --only firestore:rules
```
